<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Course Materials</title>
  <style>
    body { font-family: 'Poppins', sans-serif; background: #f8fafc; color: #1e293b; padding: 40px; }
    h1 { color: #0f172a; }
    ul { list-style: none; padding: 0; }
    li { margin: 10px 0; }
    a { color: #0ea5e9; text-decoration: none; }
    a:hover { text-decoration: underline; }
    form { margin-top: 20px; }
  </style>
</head>
<body>
  <h1>Course Materials</h1>

  <ul>
    <% if(materials.length === 0){ %>
      <li>No materials uploaded yet.</li>
    <% } %>

    <% materials.forEach(m => { %>
      <li>
        <a href="/uploads/<%= m.file_path %>" target="_blank"><%= m.file_name %></a>
      </li>
    <% }) %>
  </ul>

  <% if(user.role === 'Teacher'){ %>
    <form action="/course/<%= courseId %>/materials/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="file" required>
      <button type="submit">Upload Material</button>
    </form>
  <% } %>

  <br>
  <a href="/course/<%= courseId %>">Back to Course</a>
</body>
</html>
