
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teacher Dashboard | InnovateEd</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f8fafc;
      margin: 0;
      padding: 0;
      color: #1e293b;
    }

    header {
      background-color: white;
      padding: 20px 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    header h1 {
      font-size: 1.6rem;
      font-weight: 600;
      color: #0f172a;
    }

    header span {
      color: #0ea5e9;
      font-weight: 700;
    }

    .container {
      max-width: 1000px;
      margin: 60px auto;
      background: #ffffff;
      padding: 40px 50px;
      border-radius: 16px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    }

    .container > h1 {
      margin-top: 0;
      margin-bottom: 30px;
      text-align: center;
      font-size: 1.6rem;
    }

    h2 {
      font-size: 1.5rem;
      color: #0f172a;
      margin-bottom: 15px;
    }

    h4 {
      font-size: 1rem;
      color: #0f172a;
      margin-top: 8px;
    }

    .dashboard-grid {
      display: flex;
      gap: 60px; /* increase space between form and courses */
      align-items: flex-start;
      flex-wrap: wrap;
    }

    .form-panel {
      flex: 1 1 300px;
      max-width: 360px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    label {
      font-weight: 500;
      margin-bottom: 5px;
      display: block;
      font-size: 0.95rem;
    }

    input, textarea {
      width: 100%;
      padding: 10px 12px;
      border: 1px solid #cbd5e1;
      border-radius: 8px;
      font-size: 0.95rem;
      transition: border-color 0.3s;
    }

    input:focus, textarea:focus {
      outline: none;
      border-color: #0ea5e9;
      box-shadow: 0 0 0 3px rgba(14,165,233,0.15);
    }

    button {
      padding: 10px 0;
      background-color: #065f46;
      color: white;
      font-size: 0.95rem;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      opacity: 0.9;
      transform: translateY(-2px);
    }

    .courses-panel {
      flex: 2 1 450px; /* take more width to the right */
      margin-left: 40px; /* shift away from form */
    }

    .course-card {
      background-color: #f1f5f9;
      padding: 15px;
      border-radius: 10px;
      margin-bottom: 15px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.03);
      transition: transform 0.2s, box-shadow 0.2s;
      font-size: 0.9rem; /* reduced font size */
    }

    .course-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.08);
    }

    .course-card a {
      color: #0ea5e9;
      font-weight: 600;
      font-size: 1rem; /* slightly smaller title */
      text-decoration: none;
    }

    .course-card a:hover {
      text-decoration: underline;
    }

    ul {
      list-style: none;
      padding-left: 0;
      margin-top: 6px;
    }

    li {
      padding: 3px 0;
      color: #334155;
      font-size: 0.85rem;
    }

    p {
      color: #475569;
      font-size: 0.9rem;
    }

    .logout {
      display: inline-block;
      margin-top: 25px;
      color: #0ea5e9;
      font-weight: 500;
      text-decoration: none;
      font-size: 0.95rem;
    }

    .logout:hover {
      text-decoration: underline;
    }

    /* Responsive */
    @media (max-width: 900px) {
      .dashboard-grid {
        flex-direction: column;
      }
      .form-panel, .courses-panel {
        max-width: 100%;
        margin-left: 0;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>Welcome to <span>InnovateEd</span></h1>
    <p>📘 Learn. Grow. Succeed.</p>
  </header>

  <div class="container">
    <h1>Welcome, <%= user.full_name %> (Teacher)</h1>

    <div class="dashboard-grid">
      <!-- Left: Create Course Form -->
      <div class="form-panel">
        <h2>Create a New Course</h2>
        <form action="/create-course" method="post">
          <label for="title">Title</label>
          <input type="text" name="title" id="title" required>

          <label for="description">Description</label>
          <textarea name="description" id="description" rows="4" required></textarea>

          <label for="duration">Duration</label>
          <input type="text" name="duration" id="duration" required>

          <button type="submit">Create Course</button>
        </form>
      </div>

      <!-- Right: Courses List -->
      <div class="courses-panel">
        <h2>Your Courses and Enrolled Students</h2>
        <% if(coursesWithStudents.length > 0){ %>
          <% coursesWithStudents.forEach(course => { %>
            <div class="course-card">
              <a href="/course/<%= course.id %>">
                <strong><%= course.title %></strong> - <%= course.duration %>
              </a>
              <p><%= course.description %></p>

              <h4>Enrolled Students:</h4>
              <% if(course.students.length > 0){ %>
                <ul>
                  <% course.students.forEach(student => { %>
                    <li><%= student.full_name %> - <%= student.email %></li>
                  <% }) %>
                </ul>
              <% } else { %>
                <p>No students enrolled yet.</p>
              <% } %>
            </div>
          <% }) %>
        <% } else { %>
          <p>You have not created any courses yet.</p>
        <% } %>
      </div>
    </div>

    <a href="/logout" class="logout">Logout</a>
  </div>

</body>
</html>
